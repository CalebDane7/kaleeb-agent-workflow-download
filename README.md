# Kaleeb Agent Workflow — Encrypted Friend Bundle

This repository hosts the encrypted download only. The plaintext source remains
private.

Give your agent the stable download link and the passcode supplied separately by
the owner:

`https://github.com/CalebDane7/kaleeb-agent-workflow-download/releases/latest/download/kaleeb-agent-workflow.7z`

Tell the agent to extract it, read the included README, preview the installer for
both Codex and Claude, then apply the installation only after the dry run is safe.

The passcode is intentionally not stored in this repository or release.
